import React,{useState}from'react'
export default function Join(){const[challengeId,setChallengeId]=useState('1');const[username,setUsername]=useState('guest');const go=()=>{window.location.href=`/challenge/${challengeId}/${username}`}return(<div className="min-h-screen flex items-center justify-center p-4"><div className="w-full max-w-md bg-white p-6 rounded shadow"><h1 className="text-2xl mb-4">Join Challenge</h1><label>Challenge ID</label><input className="w-full p-2 border mb-3"value={challengeId}onChange={e=>setChallengeId(e.target.value)}/><label>Username</label><input className="w-full p-2 border mb-3"value={username}onChange={e=>setUsername(e.target.value)}/><button className="w-full bg-blue-600 text-white p-2 rounded"onClick={go}>Join</button></div></div>)}